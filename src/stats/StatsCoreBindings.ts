/** The generated wasm functions `createStatsCore` wraps. */
export type StatsCoreBindings = {
    summarize(values: Float64Array): Float64Array;
    histogram(values: Float64Array, edges: Float64Array): Uint32Array;
};
