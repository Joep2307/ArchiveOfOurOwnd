export type { BrowserTarget } from './BrowserTarget';
export {
    CONTENT_SCRIPT,
    CONTENT_STYLE,
    EXTENSION_DESCRIPTION,
    EXTENSION_NAME,
    EXTENSION_PAGES_CSP,
    EXTENSION_SHORT_NAME,
    FIREFOX_MIN_VERSION,
    GECKO_ID,
    HOST_PERMISSION,
    ICON_SIZES,
} from './constants';
export { createManifest } from './createManifest';
